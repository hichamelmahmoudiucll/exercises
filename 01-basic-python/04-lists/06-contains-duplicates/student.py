# Write your code here
def contains_duplicates(xs):
    return len(xs)!= len(set(xs)) #It converts the list to a set using set(xs), which automatically removes duplicates because sets cannot contain duplicate elements.